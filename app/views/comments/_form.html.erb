<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :User %><br>
    <%= f.text_field :user_id %>
  </p>
  <p>
    <%= f.label :Comment %><br>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>